# Session handoff 2026-07-23 — significance landed, apps folded, sharding fixed

Continues `session_2026-07-22_handoff.md`. That session ended mid-re-sweep
(`s5_exp_full_g198`, job 636570) with the significance recompute blocked. This
session ran everything downstream to completion and folded the results into both
app views. **Artifact ground truth: `~/work/prosmos_2026/INVENTORY.md`.**

## What landed

1. **Significance recompute** (`significance_expfull.json/.npz`, script
   `recompute_significance.py`, committed 07-22 in d795100). Against the corrected
   1.4M sweep, depth ratio Q = 826,903/1,191,270 = **0.694** (deep PDB; Q<1 as it
   must be). Old candidate pool 619 → **171 now PDB-lit**, **448 still-AFDB-only**;
   of the 448: Fisher-sig 18, depth-sig 74, both (defensible) 18.
2. **The five → one result.** Of the five published depth-significant cells, four
   (`sk016 ty12`, `sk158 ty27`, `sk034 ty00`, `sk033 ty07`) picked up experimental
   PDB folds once their structures were searchable → **resolved**. The lone
   survivor is **`sk171 ty07` (HHEEE**, the Ruczinski pretzel + its two helices):
   fold q = 2.05·10⁻¹¹, cluster q = 1.47·10⁻²¹, 0 PDB folds across 1.4M domains.
   This is the one cell the whole line of work points at.
3. **Cluster-unit correction** (commit a004fa5, `cluster_significance_expfull.py`,
   `build_pdb_clu50_expfull.py`, `cluster_significance_expfull.json`). Matched 50%-id/
   90%-cov mmseqs clustering BOTH sides (AFDB 1,180,565 clusters; PDB 42,834 from
   the new lit domains). Per-structure Q=0.694 (NOT families/clusters — that was
   the ~80× over-thinning error). Result: **depth-sig 185, Fisher-sig 0, both 0**.
   The matched-cluster Fisher margins shrink so no cell passes BOTH at cluster unit;
   the cluster story is therefore a *summary-level* robustness check, not a per-cell
   ranking. `sk171 ty07` survives the cluster depth test at q=1.5·10⁻²¹.
4. **Motif SVG cartoons** (commit 4d3d318, `enum/docs/motif_rendering.md`).
   `render_motif_svg.py` = 4-strand Ruczinski (all 96, DONE, faithful).
   `render_s5_iso.py` = isometric S5 with helix/strand glyphs (usable but parked —
   flat SVG primitives hit a depth-occlusion ceiling; future = real 2.5-D/3-D scene
   or molecular cartoon, NOT more primitive-tuning).
5. **Upstream sharding fixed (RS) + verified.** The 2026-07 gap-fill structures
   were mis-sharded under `uid[0:5]` instead of canonical `uid[2:7]`. RS corrected
   it upstream; verified here — all 182 experimental Ruczinski exemplars resolve
   under `[2:7]`, zero needing `[0:5]`. The app's `domainDataPath` stays
   `[2:7]`-only with **NO fallback** (a fallback would mask store rot).
6. **`/ruczinski` app** (prosmos_inspect c0e8c08). Census re-exported against
   `sweep_exp_full`: **27 exp / 47 AFDB / 20 predicted-only / 1 absent**, panel 23
   exp 21→98, `predictedInPdbDb` now uniformly 0 (dropped). Copy updated to 93.8%
   coverage; header split the 3-way partition from the overlapping "47 of 48 in
   AFDB" tally (that ambiguity was flagged and fixed).
7. **Significant browse** (prosmos_inspect bfa9488). Recomputed from exp_full:
   24 still depth-sig, 2 fisher-only still-dark, 27 **resolved** (marked
   `pdbFoldsNow/nowLit`, sorted last, excluded from the sig count, "hide resolved"
   filter). `sk171 ty07` ranks first. Per-cell cluster columns dropped (both=0).
   Annotations preserved across re-export. `data/significant/` is gitignored
   (regenerated by `export_significant.py`); only `/data/ruczinski/` is tracked.

## The census delta (why 26/47/21/1 → 27/47/20/1)

The pre-gap published census was computed with chunks missing (searchmatrix
crashes, see 07-22 handoff). Corrected: panel 12 / motif 60 crossed 0→2
experimental; zero motifs were lost. Panel 28 still absent from both.

## Key numbers to trust (all exp_full, no pre/post-gap mixing)

- Experimental coverage: **1,400,111 / 1,491,961 = 93.8%** (was 500,313 / 33.5%).
- Depth ratio Q (per-structure): **0.694**.
- AFDB-only after gap: **448** cells; defensible (fold both) **18**; depth-sig **74**.
- Original 5 depth-sig → **1 survivor** (`sk171 ty07`).
- Cluster unit: depth-sig 185, both 0.

## Open / not done

1. **Push prosmos_cl to GitHub.** main still carries the
   `PGPASSWORD='***REMOVED***'` placeholder; the branch fixed it in f141456.
2. Re-derive "panels 3/10 have no S5 parent" against graph198 (conclusion likely
   holds; the stated *reason* "grid is orientation-blind" is FALSE for the typed
   grid).
3. Regenerate `generate_s6_targeted` output against graph198 (it defaulted to
   `--matrix adjonly`); re-run s6_targeted. `extend_motif_query.py` is correct.
4. S5 isometric renderer: parked — revisit only with a real 3-D scene, not more
   primitive tuning.

## Commits this session

prosmos_cl: `a004fa5` (cluster-unit), `4d3d318` (SVG cartoons). Earlier in the
same session (07-22 handoff): `1e5546c`, `fbd3d0f`, `d795100`, `a551796`.

prosmos_inspect: `c0e8c08` (/ruczinski exp-full), `bfa9488` (significant browse).
